// JavaScript source code
import React, { Component } from "react";
/*import villageTable from "./villageTable";*/





const Compy = (props) => {
    
        return (
            <villageTable props={props} />
            //<div>
               
                
            //        {props.info.map((item) => (
            //            <div key={item.id}>
                            
            //                <li>({item.x}, {item.y }) {item.village} {item.player} { item.population } </li>
            //            </div>
            //        ))}
            //    </div>
                
        );
    }
//props.item.map((fields) => (
//    <p>{props.item} test </p>
//    ))}

export default Compy;
